# Chave exigida no header x-api-key em todas as rotas POST desta API.
# Gere uma chave forte, por exemplo: openssl rand -hex 32
# NUNCA coloque a chave real neste arquivo nem no repositório — configure-a
# apenas como variável de ambiente no Render (ou onde a API for hospedada).
PJE_API_KEY=
